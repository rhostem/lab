import React from 'react';

var CommentBox = React.createClass({
    render: function(){
        return(
          <div>{this.props.comment}</div>comp
        );
    }
});

export default CommentBox;
